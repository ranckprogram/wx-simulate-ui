import React from 'react';
import { HashRouter, Route, Switch } from 'react-router-dom';

function Router() {
    return (
        <div>
            router
        </div>
    );
}

export default Router